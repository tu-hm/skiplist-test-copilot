\* TLC model-checking configuration for SkipList.tla
\*
\* Run with:
\*   java -jar tla2tools.jar -config SkipList.cfg SkipList.tla
\*
\* With Keys={1,2,3} and Threads={1,2} TLC exhaustively explores every
\* possible interleaving of two concurrent goroutines over three keys.
\* Expected result: no errors (all Safety invariants hold, no deadlock).

\* ── Constants ──────────────────────────────────────────────────────────────
CONSTANTS
  Keys    = {1, 2, 3}
  Threads = {1, 2}

\* ── Specification entry point ──────────────────────────────────────────────
SPECIFICATION Spec

\* ── Invariants checked in every reachable state ────────────────────────────
INVARIANTS
  TypeOK
  Safety

\* ── Temporal (liveness) properties ────────────────────────────────────────
PROPERTIES
  Progress

\* ── Extra checks ───────────────────────────────────────────────────────────
\* TLC automatically checks for deadlock (a state where no action is enabled)
\* unless disabled with CHECK_DEADLOCK FALSE.  Leave it enabled.
